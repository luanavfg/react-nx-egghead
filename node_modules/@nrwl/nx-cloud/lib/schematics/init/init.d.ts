import { Schema } from './schema';
import { SchematicContext, Tree } from '@angular-devkit/schematics';
export default function init(options: Schema): () => Promise<(host: Tree, context: SchematicContext) => Promise<void>>;
